# ── Supabase ────────────────────────────────────────────────────────────────
# Project settings → API
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Server only. Never expose with a NEXT_PUBLIC_ prefix, never import in client code.
SUPABASE_SERVICE_ROLE_KEY=
# Project settings → Database → connection string (used by drizzle-kit and psql)
DATABASE_URL=

# ── App ─────────────────────────────────────────────────────────────────────
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# ── Transactional email (Phase 1 auth flows; not wired yet) ─────────────────
RESEND_API_KEY=
