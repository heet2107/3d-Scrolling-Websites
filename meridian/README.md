# MERIDIAN — wellness in rhythm with the day

An eight-page site for **MERIDIAN**, a fictional wellness practice in Ojai, California.

Static, dependency-light, no build toolchain. `meridian/` is its own deployable site
root — it carries its own `vercel.json` and is served at `/`, not under a subpath.

```bash
# Serve THIS directory as the site root, with clean-URL emulation
npx http-server meridian -p 8001 --ext html
# open http://localhost:8001
```

`--ext html` mirrors Vercel's `cleanUrls`, so `/rhythm` resolves to `rhythm.html`.

## Live

**Current branch build** (what this repository actually contains):
<https://meridian-wellness-git-cla-3d3338-heetbarot21-gmailcoms-projects.vercel.app>

**Production:** <https://meridian-wellness-rouge.vercel.app>

Those differ, and will keep differing until this branch merges. Vercel promotes to
production **only from the project's production branch** (`main`); a push to any other
branch produces a *preview* deployment and leaves the production alias untouched. Because
this project's very first deployment was marked production and nothing has been promoted
since, `meridian-wellness-rouge.vercel.app` is frozen on that first build — the one that
still had procedural artwork instead of photography.

Merging to `main` promotes automatically. To promote sooner, open the project's
Deployments tab in Vercel and use *Promote to Production* on the latest build.

The bare `meridian-wellness.vercel.app` belongs to an unrelated project — `*.vercel.app`
is a global namespace. `BASE_URL` in `build/partials.py` is the one place the canonical
origin is set; it points at the production alias deliberately, since that is the canonical
URL once merged.

## The idea

The organizing spine is the **day**, not a curriculum. Four hours, each with its own
physiology, its own practices, and its own colour:

| Hour | Window | For |
| --- | --- | --- |
| **Dawn** | 05:30–09:00 | Light, breath, waking the body |
| **Meridian** | 11:00–14:00 | Load, focus, eating properly |
| **Dusk** | 16:30–19:30 | Downregulation, sound, slow tissue |
| **Deep** | 21:00–05:00 | Sleep, repair, the actual adaptation |

That structure is load-bearing rather than decorative. **Hue equals hour** everywhere:
the ambient page wash retints as you scroll past each phase, the day-arc graphic moves a
sun along its path and recolours at each stage, and a practice card badged *Dusk* sits on
dusk-tinted artwork. `build/build.py` and `build/art.py` are kept in sync on this — there
is a check in the build notes below.

## Pages

| Page | Contains |
| --- | --- |
| `index.html` | Hero, the day arc, four phases, practices, quote, guides, journal, CTA |
| `rhythm.html` | The method in full — four phases, three principles, FAQ |
| `practices.html` | Breath, Movement, Sound, Restore, Nourish — cards plus alternating detail |
| `retreats.html` | Three residential retreats and a sample retreat day |
| `guides.html` | Four phase guides plus the wider practice |
| `journal.html` | Featured piece, archive rows, newsletter panel |
| `membership.html` | Three tiers (middle highlighted), what's included, FAQ |
| `visit.html` | Weekly schedule, stylised map, booking form |

## Type & colour

Warm off-white ground (`#FBF8F3`) with near-black warm ink and **sage** (`#5E7154`) as the
one constant accent; the four phase hues are the variable. Display face is **Fraunces**
(variable, with its `SOFT` and `WONK` axes turned up), body is **DM Sans**. Both are
self-hosted latin subsets — 424 KB, no CDN.

## Imagery

**Photographs**, in `assets/img/` — 26 files sourced from Wikimedia Commons, all public
domain or Creative Commons, then cropped and colour graded to one palette (saturation down,
warm push, blacks lifted) so twenty-odd mixed-provenance images read as a single body of
work. Full attribution and the share-alike obligations are in [CREDITS.md](CREDITS.md).

The art direction follows the source. Commons is strong on landscape and natural light and
weak on interiors, studios and food — and MERIDIAN is a site about the light of the day, so
the photography *is* the light of the day. Each phase image is that hour's light.

**Procedural artwork**, in `assets/art/` — only the four guide slots, generated by
`build/art.py` (seeded, deterministic).

Guides stay abstract on purpose. They are invented people with invented names and
biographies, and attaching those to a photograph of a real, identifiable person
misrepresents them. Shoot or commission real portraits and point those four slots at them.

```bash
python3 build/art.py     # regenerate the four guide artworks
python3 build/build.py   # regenerate the eight pages, sitemap and robots.txt
```

Run both **from this directory**, never from the repository root — the root has its own
`build/build.py` for the other site, and running that one strips the image dimensions
`stamp_dimensions.py` adds.

## Motion

GSAP + ScrollTrigger, vendored in `assets/js/vendor/` (no CDN).

- **Ambient phase light** — whichever hour is nearest the middle of the viewport sets
  `--phase` / `--phase-wash` on `<html>`; the page warms and cools as you read.
- **The day arc** — a scrubbed sun travels the path, drawing the trail behind it and
  recolouring at each phase.
- **Hero** — word-by-word rise on load.
- **Cards, tiers, rows** — CSS hover lift with artwork zoom. Reveals hand elements back
  to CSS on completion (`clearProps`), because GSAP's leftover inline transform otherwise
  outranks `:hover` and silently kills the lift.
- **Counters** — with years excluded, since counting up to "2016" reads as nonsense.

Everything is applied *from* JavaScript, so pages render complete with JS blocked, and all
motion is skipped under `prefers-reduced-motion`.

## Mobile

Below 860px card grids become swipeable snap rails with dot indicators, the nav collapses
to a sheet (its CTA is hidden — the sheet carries its own), phase blocks stack with the
artwork on top, and pricing stacks with the middle tier still flagged.

## Content

Everything is invented — the practice, its guides, the address, the prices, the schedule.
The phone number uses the 555 reserved range. Both forms are front-end only.
